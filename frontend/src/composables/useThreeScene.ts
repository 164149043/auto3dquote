/**
 * Three.js 3D 模型预览逻辑已内联到 ModelPreview.vue 组件中。
 * 如需复用，可从此处抽取 scene/camera/renderer 的初始化逻辑。
 */

// 当前 ModelPreview.vue 直接包含 Three.js 逻辑，无需额外 composable。
// 如未来需要多个 3D 视图，可在此抽取通用场景管理器。

export {}
